[build]
  # Solaris Letters React client
  base    = "Solaris Letters/client"
  command = "npm run build"
  publish = "Solaris Letters/client/dist"

# SPA redirect — all client-side routes serve index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200