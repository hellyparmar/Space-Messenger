[build]
  # Tell Netlify the app lives in this subdirectory
  base    = "Solaris Letters/client"
  command = "npm run build"
  publish = "dist"

# SPA redirect — all client-side routes serve index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
